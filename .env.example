# ---- Alpaca ----
# During pre-hackathon dev, use any paper account for testing.
# On Day 1 of the hackathon (Aug 28, 2026) create a BRAND NEW paper account
# dedicated to the submission and swap these keys.
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_PAPER=true
ALPACA_TRADE_API_URL=https://paper-api.alpaca.markets
ALPACA_DATA_API_URL=https://data.alpaca.markets

# Path to a local checkout of https://github.com/alpacahq/alpaca-mcp-server
# (see PLAN.md > Setup for clone instructions)
ALPACA_MCP_SERVER_PATH=../alpaca-mcp-server

# ---- LLM ----
# provider = featherless | fireworks | anthropic
LLM_PROVIDER=featherless
LLM_MODEL=moonshotai/Kimi-K2-Instruct
LLM_FALLBACK_MODEL=deepseek-ai/DeepSeek-V3

FEATHERLESS_API_KEY=
FEATHERLESS_BASE_URL=https://api.featherless.ai/v1

FIREWORKS_API_KEY=
FIREWORKS_BASE_URL=https://api.fireworks.ai/inference/v1

# Optional — only used if LLM_PROVIDER=anthropic (incurs API cost)
ANTHROPIC_API_KEY=

# ---- Database ----
# SQLite for the hackathon build. To move to Supabase later, swap this for
# your Supabase Postgres connection string (postgresql+psycopg2://...) —
# no code changes needed, models are provider-agnostic.
DATABASE_URL=sqlite:///./deltamind.db

# ---- App ----
APP_ENV=development
LOG_LEVEL=INFO
BACKEND_CORS_ORIGINS=http://localhost:5173
