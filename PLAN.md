# DeltaMind AI — Plan & Context

**This is the living source of truth for the project.** Update it as decisions change — don't let it drift from reality. Everything else (code, memory) can be re-derived; this file carries the *why*.

Last updated: 2026-08-24 (session 5)

---

## 1. What this is

An autonomous multi-agent options-trading system built for the **Alpaca AI Trading Agents Hackathon** (lablab.ai × Alpaca).

- **Event window:** Aug 28 – Sep 4, 2026 (7 days, online)
- **Prize pool:** $5,000 total ($2,500 / $1,500 / $1,000 top 3), funded by AlpacaDB
- **What's judged:** paper-trading P&L, technology implementation (Alpaca Trading API / MCP Server v2 / CLI depth), creativity, presentation, and build-in-public social engagement
- **Core constraint:** the agent must run *live* on a **brand-new** Alpaca paper account created on Day 1 (Aug 28) — no pre-existing trade history, no trades before the account exists. Everything else (scaffolding, math libraries, backtesting, UI) can be built now.

Full hackathon rules and strategy detail are in the brief the user provided at project kickoff (not duplicated here — see chat history / lablab.ai listing for the source).

---

## 2. Timeline

| Phase | Dates | What happens |
|---|---|---|
| **Pre-hackathon (now)** | 2026-08-19 → 2026-08-27 | Build everything in this repo: backend scaffolding, quant math, agent graph, UI, backtester. Test against a *throwaway* dev paper account — never the submission account. |
| **Day 1** | 2026-08-28 | Register the fresh submission paper account. Swap `.env` credentials. Deploy a Minimum Viable Agent immediately — don't wait for a "perfect" system. |
| **Days 2–6** | 2026-08-29 → 2026-09-02 | Keep the live loop running continuously (accumulating real trade history for judging) while refining prompts/signals/reasoning in the background. Post build-in-public updates on X/LinkedIn (up to 5, tagging @lablabai and @AlpacaHQ). |
| **Day 6 wrap** | 2026-09-03 | Freeze trading code. Record the 5-minute demo video. Export slide deck to PDF. Clean up the GitHub repo. |
| **Day 7** | 2026-09-04 | Submit via lablab.ai dashboard before deadline. |

**Why deploy Day 1, not Day 7:** judges evaluate trade *history*, not just a final number — a system with one lucky trade on the last day scores poorly on both P&L consistency and Technology Implementation.

---

## 3. Architecture

Multi-agent pipeline, orchestrated as a LangGraph state graph, one cycle per watched symbol:

```
market_ingestion → quant_engine → news_analyst → lead_architect → risk_gate
                                                                       │
                                                        risk_approved? │
                                                    yes ┌──────────────┴──────────────┐ no
                                                        ▼                             ▼
                                                    execution                    END (logged,
                                                        │                        no trade)
                                                        ▼
                                                       END
```

| Node | File | LLM? | Responsibility |
|---|---|---|---|
| Market Ingestion | `backend/app/agents/market_ingestion.py` | No | Pull bars + option chain via Alpaca REST |
| Quant Engine | `backend/app/agents/quant_engine.py` | **No — deterministic** | Greeks, IV percentile, portfolio risk. Pure math, auditable, reproducible. |
| News Analyst | `backend/app/agents/news_analyst.py` | Yes | Sentiment score from headlines (via MCP `get_news`) |
| Lead Architect | `backend/app/agents/lead_architect.py` | Yes | Synthesizes quant + sentiment into a thesis, delegates to the active track's strategy module for an order proposal |
| Risk Gate | `backend/app/agents/risk_gate.py` | **No — deterministic circuit breaker** | Hard guardrails: max position size, margin utilization, mandatory stop-loss, order structure validation |
| Execution | `backend/app/agents/execution.py` | No | Dispatches the approved order via the Alpaca MCP client, persists the result |

**Why quant + risk gate are LLM-free:** judges need to be able to read those two files and verify the guardrails hold — no prompt-injection or hallucination surface on the parts of the system that touch capital directly.

---

## 4. Tech stack decisions

| Area | Choice | Why |
|---|---|---|
| Agent orchestration | **LangGraph** | Explicit state graph matches the strict linear pipeline above; keeps the deterministic risk gate cleanly separated from LLM nodes; good tracing for the demo/judges. |
| Backend API | **FastAPI** | Async-native, built-in WebSocket support for streaming live agent/trade events to the dashboard, Pydantic models pair naturally with MCP tool schemas. |
| LLM provider | **Featherless / Fireworks (OpenAI-compatible endpoints)**, selectable per-provider via the Controls tab, 4 models offered on each: **Kimi K2 Instruct (default), DeepSeek-V3, Qwen2.5 72B Instruct, Llama 3.3 70B Instruct** | Anthropic API billing is **separate from the Claude Code subscription** — direct API calls cost extra per token. User has free Featherless + Fireworks credits, so the reasoning nodes default there at $0 marginal cost. Client is provider-agnostic (`backend/app/llm/client.py`) — an Anthropic adapter is wired in and can be flipped on later if quality demands it and cost is acceptable. Model list in `frontend/src/features/controls/models.ts`, cross-checked against real slugs used in two of the user's other projects (Auditflow's `featherless.js`, Aero_claw's `server.cjs`) plus a live catalog fetch. **Always confirm with the Controls tab's "Test API" button before relying on a model live — provider catalogs drift.** |
| Database | **SQLite** now, **Supabase Postgres**-ready later | Zero setup, easy for judges to inspect (`deltamind.db` ships in the repo/demo), plenty for a week of trade volume. All access goes through `backend/app/db/repository.py`, and models are plain SQLAlchemy — pointing `DATABASE_URL` at the user's existing Supabase instance post-hackathon is a config change, not a rewrite. |
| Frontend | **React + TypeScript + Vite** | Per user requirement. Talks to FastAPI over REST (`/api/*`) and WebSocket (`/ws/live`) for the live reasoning feed. |
| Market/execution interface | **Alpaca MCP Server v2** for execution + news; **alpaca-py REST** for high-frequency data pulls and backtesting | MCP gives every live order an auditable tool-call trail (scores well on Technology Implementation); going through an LLM/MCP round-trip for routine bar polling would be pure overhead, so that stays on direct REST. CLI wrapper (`backend/app/alpaca/cli_wrapper.py`) is scaffolded for future low-overhead cron/health-check use. |

---

## 5. Open decision: which hackathon track?

The four tracks (see brief) each map to one strategy module, all already scaffolded in `backend/app/strategies/`:

| Track | Module | Structure | Best regime |
|---|---|---|---|
| 1. Options Alpha | `track1_alpha_spreads.py` | Vertical debit spreads (0.70Δ long / 0.30Δ long-side, 14 DTE) | Strong trend/momentum |
| 2. Volatility & Events | `track2_volatility_events.py` | Long strangle (IV%<25) / iron condor (IV%>85) around earnings | Pre-earnings / macro events |
| 3. Hedging & Protection | `track3_hedging.py` | Collar triggered at >3.5% portfolio drawdown | High volatility / drawdowns |
| 4. Income & Overlay | `track4_income_wheel.py` | The Wheel — 0.30Δ cash-secured puts → covered calls | Sideways/range-bound |

**Decided: Track 1 (Options Alpha) is primary, Track 4 (Income/Wheel) is the committed secondary**, to be perfected in that order — both fully tested (not just demoed) before the hackathon, with a third (Track 2) only if time remains and the earnings/macro calendar for Aug 28–Sep 4 actually cooperates. Reasoning for the pairing, not just deferring to a suggestion:

- **Regime-complementary, not redundant.** Track 1 needs a trend (breakout + sentiment agreeing) — in a quiet week it could go days without a qualifying setup, which hurts the "trade frequency/consistency" judging criteria on its own. Track 4 needs no trend at all, just an underlying worth selling premium on, which exists every day. Running both means real trade history accumulates regardless of which regime the week turns out to be.
- **Track 2 is calendar-dependent in a way the other three aren't** — its whole thesis is "IV expansion/crush around a scheduled event," so it only fires if something real (earnings, a macro print) actually lands during Aug 28–Sep 4. Worth checking that calendar before promoting it above Track 4, not assuming it'll have opportunities to trade.
- **Track 3 is the weakest fit for a one-week demo**, not deprioritized arbitrarily: its trigger is portfolio drawdown on an *existing* equity position — without one already established and losing >3.5%, it may simply never fire, which is worse for judging than any of the other three even trading occasionally.

**All four modules share the same `propose_order(state, thesis) -> order` interface**, so switching — or running multiple tracks on different symbols simultaneously — is a config change (the Controls tab's Track dropdown, or `run_agent_loop.py --track`), not a rewrite. Per-track explainer docs (mechanics, options math, the LLM-vs-deterministic-code split, known gaps) live in `docs/tracks/` — see [00_options_basics.md](docs/tracks/00_options_basics.md), [track1_alpha_spreads.md](docs/tracks/track1_alpha_spreads.md), [track4_income_wheel.md](docs/tracks/track4_income_wheel.md) — written for both the user's own understanding and reuse in the judge-facing submission writeup. A Track 2 doc is deferred until it's actually committed as the third track.

---

## 6. UI design system

The dashboard's visual identity is deliberately ported from a sibling project's proven design system (same component architecture, same Tailwind-v4-tokens-as-CSS-variables approach, same font pairing) — re-themed, not re-designed from scratch, and re-skinned with DeltaMind's own colors and copy.

| Aspect | This project | Ported from | Why |
|---|---|---|---|
| Palette | Charcoal black `#121212` (dark, default) / premium off-white `#faf9f7` (light), single accent **International Orange (Aerospace) `#FF4F00`** in both themes | Sibling project's charcoal-black / cream palette with a gold accent | User explicitly requested this swap — same structure, different identity. Hex confirmed via web search, not guessed. |
| Token file | `frontend/src/styles/theme.css` — the single editable source for every color and font; a `:root` block (light) and a `.dark` block (dark) feed a Tailwind v4 `@theme` block, so editing a hex here propagates everywhere with no component changes | Same `:root`/`.dark`/`@theme` structure | This is the "themes file" the user asked for — swap the accent or the whole palette in one file. |
| Fonts | Fraunces (display/headline), IBM Plex Sans (UI), IBM Plex Mono (data/tabular), Wallpoet (logo mark only) | Identical pairing | Kept as-is — it's part of what made the reference project's style read as "premium instrument panel," not something specific to that project's brand. |
| Components | `frontend/src/components/primitives.tsx` — Section, Card, Button, TextField, Select, StatTile, StatusDot, DecisionRow, PositionRow, Spinner | Same primitives, same Tailwind utility patterns; `SignalRow`→`DecisionRow`, `PositionRow` re-mapped to Alpaca's Account/Position shape | Structural pattern reused; field names and data are 100% DeltaMind's own — nothing about options trading was copied, only "how a card/button/status-dot looks." |
| Pages | Landing (Console gate) → 4-tab app shell (Controls / Strategies / Backtest / Logs) | Same shell shape | Controls = AI Configuration + Telemetry + **Agent Engine** (renamed from "Server Engine" — controls `scripts/run_agent_loop.py` as a subprocess, not a second web server) + Live Trading Activity. Strategies = read-only reference cards for the 4 tracks (not the original's image-template editor — different domain entirely). Backtest = form + stat tiles. Logs = byte-offset-tailed log viewer parsing the same `%(asctime)s - %(name)s - %(levelname)s - %(message)s` format. |
| Landing page | CSS/SVG grid-plus-price-line backdrop with an accent glow (no photo asset) | Same layout shell (logo, tagline, headline, CTA, footer), photo backdrop swapped for a generated one since there's no cover image for this project | "Copy the style, not the assets." |

**LLM model catalog** (`frontend/src/features/controls/models.ts`): 4 models offered identically on both Featherless and Fireworks — **Kimi K2 Instruct** (default, strongest agentic/tool-calling fit), **DeepSeek-V3**, **Qwen2.5 72B Instruct**, **Llama 3.3 70B Instruct**. Cross-checked against real slugs already working in two of the user's other projects (Auditflow's `src/services/featherless.js`, Aero_claw's `app/server.cjs`) plus a live catalog fetch from both providers. Exact slugs drift — the Controls tab's **Test API** button is the actual verification step before trusting one live, same as `Test Alpaca Connection` for brokerage credentials.

**Config persistence** (`backend/app/config_store.py`): the Controls tab's "Save Changes" writes to `backend/.runtime_config.json` (gitignored), **not** `.env` — `.env` stays the source of truth for Alpaca credentials and initial defaults for a headless Day-1 deploy that never touches the UI. API keys are encrypted at rest with **Windows DPAPI** (bound to this Windows user account, same approach as a sibling project's `config_manager.py`), with a plaintext fallback + warning off Windows so the app doesn't crash in that case. Verified end-to-end: a saved key round-trips correctly through the API and is genuinely encrypted on disk (confirmed by inspecting the raw JSON file).

**Agent Engine lifecycle** (`backend/app/agent_loop_manager.py`): Start/Stop/Restart launch `scripts/run_agent_loop.py` as a real subprocess (`subprocess.Popen`, not a thread), tracked by PID; Stop sends SIGTERM with a 5s grace period before SIGKILL. Verified live: started a real subprocess, confirmed PID tracking, watched it correctly fail-and-continue through repeated cycles against fake credentials (proves the try/except-and-keep-looping design in `run_agent_loop.py` actually holds up), and cleanly stopped it. Logging is configured *before* the ~10s cold import of LangGraph/alpaca-py/LLM SDKs so the Logs tab shows activity immediately instead of looking dead during that window.

---

## 7. Repo structure

```
DeltaMind_ai/
├── PLAN.md                  ← you are here
├── README.md
├── .env.example
├── docs/tracks/             ← per-track explainer docs (section 5): options basics, mechanics, LLM-vs-code split, gaps
├── backend/                 Python 3.11+, managed with uv
│   ├── pyproject.toml
│   ├── .runtime_config.json     (gitignored — Controls tab's saved config, DPAPI-encrypted keys)
│   ├── logs/agent_loop.log      (gitignored — tailed by the Logs tab)
│   ├── app/
│   │   ├── main.py          FastAPI app + router wiring
│   │   ├── config.py        pydantic-settings (.env — defaults/fallback)
│   │   ├── config_store.py  Controls-tab runtime config, DPAPI-encrypted at rest (section 6)
│   │   ├── agent_loop_manager.py  subprocess lifecycle for the live loop (section 6)
│   │   ├── api/             REST routes: account/positions/trades/backtest/config/engine/logs + /ws/live
│   │   ├── agents/          LangGraph nodes + graph.py wiring (section 3); position_monitor.py — plain-function exit/assignment sweep (session 4)
│   │   ├── llm/             provider-agnostic LLM client (section 4)
│   │   ├── alpaca/          mcp_client.py, rest_client.py (get_option_chain joins two Alpaca APIs — session 4), cli_wrapper.py
│   │   ├── quant/           black_scholes.py, greeks.py, iv_percentile.py, risk_metrics.py, technical_signals.py — pure math, unit-tested
│   │   ├── strategies/      one module per track (section 5); _common.py's closest_by_delta/net_debit_credit shared by all
│   │   ├── watchlist.py     curated 15-symbol universe + sector map + liquidity filter (session 4)
│   │   ├── db/              SQLAlchemy models + repository.py (data-access layer)
│   │   └── backtest/        data_loader.py, engine.py (stub — see TODOs), runner.py
│   ├── scripts/
│   │   └── run_agent_loop.py   ← live-loop entrypoint, launched by agent_loop_manager.py
│   └── tests/
│       ├── test_black_scholes.py
│       └── test_strategies_common.py   DTE/sign-fix logic, net_debit_credit (session 4)
└── frontend/                 React + TypeScript + Vite + Tailwind v4
    └── src/
        ├── api/               client.ts (typed REST client), types.ts
        ├── styles/theme.css   ← THE editable design-tokens file (section 6)
        ├── components/primitives.tsx   Section, Card, Button, TextField, Select, StatTile, StatusDot, DecisionRow, PositionRow
        ├── features/
        │   ├── landing/LandingPage.tsx
        │   ├── controls/      ControlsPage.tsx, models.ts (LLM provider/model catalog)
        │   ├── strategies/StrategiesPage.tsx
        │   ├── backtest/BacktestPage.tsx
        │   └── logs/LogsPage.tsx
        └── App.tsx            landing/app shell, 4-tab nav, theme toggle
```

---

## 8. Setup

### Backend
```bash
cd backend
uv venv
uv pip install -e ".[dev]"
cp ../.env.example ../.env   # fill in keys — see below
uv run uvicorn app.main:app --reload
```

### Frontend
```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

### Alpaca MCP Server v2 (required for live execution + news)
```bash
git clone https://github.com/alpacahq/alpaca-mcp-server ../alpaca-mcp-server
cd ../alpaca-mcp-server
uv sync
```
Point `ALPACA_MCP_SERVER_PATH` in `.env` at that checkout. `backend/app/alpaca/mcp_client.py` spawns it as a subprocess over stdio.

### LLM credentials
Either fill in `FEATHERLESS_API_KEY` / `FIREWORKS_API_KEY` in `.env` (used for a headless start), **or** open the dashboard's Controls tab, pick a provider/model from the dropdown, paste the key, hit **Test API**, then **Save Changes** — this writes to `backend/.runtime_config.json` (DPAPI-encrypted) and takes effect on the agent's next cycle without a restart.

### Day 1 checklist (do NOT do this early — see rules in section 1)
1. Register a **brand-new** Alpaca paper trading account.
2. Update `ALPACA_API_KEY` / `ALPACA_SECRET_KEY` in the production `.env` to that account.
3. Record the account ID — required in the final submission form.
4. Set Symbols/Track/Interval on the Controls tab (or via `.env`/CLI) and hit **Start** — or run `scripts/run_agent_loop.py` directly for a fully headless deploy.

---

## 9. Risk guardrails (enforced in `risk_gate.py`, configurable in `config.py`)

- Max single position: **10%** of portfolio equity
- Max margin utilization: **50%**
- Every proposed order must carry a stop-loss ≤ **20%** (track modules set their own, tighter, defaults)
- Order structure validated (symbol + legs present) before anything reaches Alpaca

---

## 10. Submission checklist (fill in as completed)

- [ ] Project title + short description (≤255 chars) + long description (≥100 words)
- [ ] Public GitHub repo, MIT-licensed, cleaned up
- [ ] Hosted dashboard URL (frontend deploy target TBD — Vercel is the likely default for a Vite app; backend needs a host too, e.g. Railway/Render/Fly — **decide closer to Day 6**)
- [ ] Fresh Alpaca paper account ID
- [ ] 5-minute video (MP4 or YouTube link)
- [ ] Slide deck (PDF)
- [ ] Up to 5 social posts (X/LinkedIn, tagging @lablabai and @AlpacaHQ)

---

## 11. Open questions / TODOs

Track 1 and Track 4's documented gaps from session 3 are now closed (bear put spread, exit-management monitor, IV screening, DTE targeting, assignment detection — see section 12's session 4 entry for what changed and how each was verified). Session 5 closed rate limiting, local crash-recovery, and Track 1's threshold-strictness gaps — see that entry. Remaining:

- **No real order has been submitted end-to-end yet.** Every live test so far correctly rejected on "no breakout confirmed" / "sentiment insufficient." Track 1's thresholds are now moderate-loosened by default (±0.5 sentiment, 1.2x volume, live-tunable in Controls) specifically to address this for the 3-day test window — outcome unknown until real testing runs. `place_option_order`'s MCP argument shape, `execution.py`'s leg-translation, and `position_monitor.py`'s closing-order path are all built and unit/integration-tested against real chain data, but **none have been exercised by an actual accepted order yet**.
- **`backend/app/backtest/engine.py`** is still a stub — needs wiring to `data_loader.py` + the strategy modules once real historical option-chain-with-Greeks access is confirmed against the user's Alpaca data plan/entitlements.
- **`get_iv_52wk_range()`** in `db/repository.py` returns a placeholder range — needs a real historical-IV table populated from backtest data before `iv_percentile.py` output is trustworthy.
- **Track 2's IV-percentile trigger has no earnings/macro-calendar check** (found during session 3's track review, same shape as Track 1's original breakout gap) — out of scope while Track 2 stays uncommitted, but relevant if it gets promoted to a third track around the Sep 4 jobs report (see section 5).
- **Hosting** — local for the 3-day test window (session 5 added crash-recovery + sleep-prevention, see `docs/local_reliability.md`); a Render cloud deployment is being prepared in parallel (not cut over to yet) — see `docs/deployment.md` once session 5's background prep work lands. Full reboot coverage (Task Scheduler) was deliberately skipped for this window — only subprocess crash-recovery is automatic, not a full Windows reboot.
- **LLM model slugs** — the Controls tab dropdown lists 4 candidates per provider (section 6); **Kimi K2 Instruct is now confirmed live** on Featherless (`moonshotai/Kimi-K2-Instruct`) — the other 3 (DeepSeek-V3, Qwen2.5 72B, Llama 3.3 70B) and the Fireworks-side slugs are still unverified. Confirm with **Test API** before switching to one.
- **`open_interest` availability on live paper data turned out better than initially feared** — real, non-null values were returned in testing (see session 4), but this was flagged as potentially unreliable per Alpaca's own docs; don't assume it's always populated for every contract/symbol.
- **Track 4 has no realized-P&L exit path yet** (found while building session 5's Track P&L summary) — `position_monitor.py`'s sweep only ever calls `close_trade()` for Track 1; Track 4's CSP/covered-call trades never transition out of `status="open"` today, so Track 4's realized P&L will read $0 even after a real assignment/expiration cycle. Unrealized P&L for Track 4 is a best-effort estimate (`pnl_utils.compute_unrealized_pnl`) that may undercount mid-assignment. Not fixed this session — flagged, not blocking the 3-day window, but worth closing before treating Track 4's numbers as trustworthy for judging.
- **`config_store.py`'s DPAPI key encryption is Windows-only** — silently falls back to plaintext storage on Linux (a real concern only once/if the Render cutover happens; mitigation documented in `docs/deployment.md`, not code-guarded yet).
- No additional Claude Code skills needed beyond what's already installed (`dataviz` flagged inline in `BacktestPage.tsx`/`ControlsPage.tsx` comments for whenever a real equity-curve chart gets built).

---

## 12. Status log

- **2026-08-19 (session 1)** — Repo scaffolded from scratch: backend (FastAPI + LangGraph agent pipeline + quant math + SQLite persistence), frontend (React/TS/Vite dashboard skeleton), full architecture and tech-stack decisions recorded above.
  - `git init` done at repo root (not yet committed — nothing has been committed anywhere in this project).
  - Cloned `alpacahq/alpaca-mcp-server` to `../alpaca-mcp-server` (sibling of this repo), matching the default `ALPACA_MCP_SERVER_PATH`.
  - Verified end-to-end: fixed a hatchling build-config gap (`pyproject.toml` needed `[tool.hatch.build.targets.wheel] packages = ["app"]` since the package dir is named `app`, not `deltamind_backend`), then `uv venv` (auto-fetched Python 3.11.5), `uv pip install -e ".[dev]"`, `pytest` (3/3 pass on the Black-Scholes/Greeks module), `app.main` imports cleanly with all routes registered, the LangGraph pipeline compiles with the exact intended edge structure, and `uvicorn` boots and serves `/api/health` successfully (confirms SQLite `init_db()` startup hook works — `deltamind.db` created).
  - Frontend: `npm install` (68 packages) and `tsc -b` both clean, no type errors.

- **2026-08-19 (session 2)** — Confirmed **Track 1** as the primary submission track (section 5). Rebuilt the frontend as a full dashboard styled after a sibling project's design system (section 6): Tailwind v4 + `theme.css` design tokens (charcoal black / premium white, International Orange `#FF4F00` accent), ported component primitives, and a 4-tab app shell (Controls / Strategies / Backtest / Logs) behind a landing page.
  - Added the LLM model catalog (`models.ts`): Kimi K2 Instruct, DeepSeek-V3, Qwen2.5 72B, Llama 3.3 70B — available on both Featherless and Fireworks, cross-checked against real slugs from the user's Auditflow/Aero_claw projects plus a live catalog fetch.
  - Added backend support for the new Controls tab: `config_store.py` (DPAPI-encrypted runtime config, separate from `.env`), `agent_loop_manager.py` (subprocess lifecycle for the live loop), and `routes_config.py` / `routes_engine.py` / `routes_logs.py`. `llm/providers.py` and `alpaca/rest_client.py` gained `test_provider()` / `test_connection()` helpers backing the Test API / Test Alpaca Connection buttons. `run_agent_loop.py` now logs to `logs/agent_loop.log` in the format the Logs tab parses, with logging configured *before* the ~10s cold import of LangGraph/alpaca-py/LLM SDKs so the tab doesn't look dead during startup.
  - Verified thoroughly, not just compiled: `tsc -b` clean, all new API routes registered (checked via the OpenAPI schema), config save/load round-trips correctly with the API key genuinely DPAPI-encrypted on disk (inspected the raw file), a real agent-loop subprocess was started/tracked-by-PID/stopped, and — with fake credentials — correctly logged graceful per-cycle errors and kept retrying at the configured interval rather than crashing. Screenshotted all 4 tabs plus the landing page in a headless Chrome instance (driven via raw Chrome DevTools Protocol over the already-installed `websockets` package, since neither `chromium-cli` nor Playwright were available in this environment) — zero browser console errors, layout matches the reference screenshots' structure while using DeltaMind's own colors/copy/fields.
  - **Not yet done:** no real Alpaca or LLM credentials configured anywhere (see section 11); `npm audit` flagged 2 vulnerabilities (1 moderate, 1 high) in dev-only deps, not yet reviewed; nothing committed to git yet.

- **2026-08-23 (session 3)** — Real credentials in hand: Alpaca paper account (key `PKNTVFWK2643TPPQXA375G2EA2`, dev/test account — **still needs a brand-new one on Day 1** per section 1) and a Featherless API key. Written to `backend/.env` (confirmed gitignored via `git check-ignore`, not staged).
  - Registered Alpaca's Trading MCP Server v2 for Claude Code itself: `claude mcp add alpaca` at **local scope** (deliberately not the doc's suggested `--scope user` global scope, and not `project` scope either — local keeps the API keys out of any file git could ever pick up, stored in the user's own `~/.claude.json` instead). Launched via `uvx alpaca-mcp-server` (the officially documented PyPI-package approach) — confirmed connected via `claude mcp list` after warming the `uvx` package cache (first run downloads ~74 packages, which made the first couple of health-checks time out before the cache was warm). Its tools aren't callable by *me* mid-conversation since it was added after this session started — available to Claude Code from the next fresh session on this project.
  - Rewrote `mcp_client.py`'s launch mechanism to match: `uvx alpaca-mcp-server` instead of a local git-clone path (`ALPACA_MCP_SERVER_PATH` config removed as dead weight). Confirmed the real multi-leg option order shape against alpaca-py's own `OptionLegRequest`/`OrderRequest` models (found in the installed SDK, since the MCP server is generated from these) rather than guessing from the hackathon brief.
  - **Verified real credentials end-to-end, not just "saved":** `/api/config/test-alpaca` connected to the real paper account (`PA31AXNLB6OS`, $100,000 equity); `/api/config/test-llm` got a real reply from Featherless/Kimi K2 (took ~60-90s — Featherless's serverless cold-start for spinning up a model, not a bug). Then ran one full live agent cycle (SPY, Track 1) against both real credentials together — **caught and fixed a real bug in the process**: `mcp_client._call_tool()` assumed the MCP SDK's `CallToolResult` was dict-like; it's actually `.content` (a list of content blocks) plus `.structured_content`/`.is_error` (both confirmed snake_case by reading the installed SDK's actual source after the live call crashed on it). Fixed to prefer `structured_content`, falling back to parsing the first text block as JSON. Re-ran after the fix: the full pipeline completed clean — real Alpaca market data → real MCP `get_news` call → two real Featherless/Kimi K2 LLM calls (sentiment + thesis) → risk gate correctly rejected (no order proposed) → decision persisted to SQLite, all confirmed via `/api/trades/decisions` and `/api/engine/stats`.
  - **Found (not yet fixed):** the LLM's thesis text reasoned about IV percentile/vega/a long straddle while running under Track 1 — that's Track 2's structure. See section 11 — needs a prompt fix before trusting live thesis output to stay on-track.

- **2026-08-26 (session 6, continued) — per-track Alpaca account separation.** Track 1 went live-tested (websocket confirmed connected, warmup pass + bar-close events firing correctly, zero LLM calls burned on unqualified symbols) then paused for the day (market closed) with the engine stopped. Track 4 was then fully streamlined against a user-supplied Wheel-strategy spec (IV percentile floor, daily-bar 200-EMA/RSI regime gate, merged LLM risk-officer validator gated behind the deterministic screen, real profit-target/stop-loss/assignment exit sweep, a new self-bootstrapping IV-history table replacing a hardcoded placeholder, and — the actual blocker found along the way — a separate `max_wheel_collateral_pct` (25%) since the shared 3% cap rejected every symbol, even the cheapest ones, once CSP collateral (strike × 100) was checked against real current prices).
  - **New requirement surfaced by the user querying "how do I raise the paper account balance?" and independently realizing the real constraint**: hackathon submissions score each track independently, so Track 1 and Track 4 cannot share one Alpaca account once a real submission is being prepared — two separate accounts are required, one per track.
  - **Implemented the plumbing now, with zero behavior change until the user acts**: `app/config.py` gained `alpaca_api_key_track4`/`alpaca_secret_key_track4` (both empty by default) and a `get_alpaca_credentials(track)` resolver — Track 4 uses its own pair once *both* are set in `.env`, otherwise falls back to the shared pair, so today's single-account testing needs no changes at all. `agent_loop_manager.py`'s `_spawn()` now passes the resolved pair as that subprocess's own `ALPACA_API_KEY`/`ALPACA_SECRET_KEY` env vars — works cleanly with zero changes to `rest_client.py`'s cached client singletons or `mcp_client.py`'s MCP-subprocess env, since each track already runs as a fully separate OS process (a deliberate choice from Track 1's redesign) and each process's own `Settings()` just resolves against its own env. `rest_client.test_connection()` and `/api/config/test-alpaca` now take an optional per-track credential pair (building a one-off uncached `TradingClient` when testing a non-default account), and the Controls UI's "Test Alpaca Connection" button now tests whichever track's tab it's clicked from. `.env.example`, `backend/.env`, and `docs/deployment.md`'s cutover table updated to document the two-separate-accounts requirement for Day 1 (2026-08-28).
  - **Still open, user's manual step**: create the two new paper accounts and set `ALPACA_API_KEY_TRACK4`/`ALPACA_SECRET_KEY_TRACK4` (plus swap the default pair to a fresh Track 1 account) before the actual hackathon submission — nothing in the code depends on this happening before then. Also unresolved: where/whether Alpaca's dashboard still exposes a paper-balance reset control (user couldn't find it) — worth revisiting once the two new accounts are actually created, since SPY/QQQ-class collateral only fits comfortably under the new 25% cap with a larger-than-$100k paper balance.

  - **Fixed the track-drift bug**, and two more found in the process of writing it up properly:
    - `lead_architect.py`'s prompt now names the active track explicitly (`TRACK_ENTRY_RULES`) and instructs the model to say "no trade this cycle" rather than describe a different track's structure when the numbers don't fit its own. **Confirmed live, before/after**: same market conditions, old thesis was *"...initiate a 30-day long straddle to capture vega gains..."* (Track 2's structure, wrong), new thesis is *"No breakout confirmed; sentiment score of 0.0 lacks directional conviction beyond ±0.75 threshold — no trade this cycle."* (correct).
    - **Track 1's entry condition was only half-implemented**: the docstring and hackathon brief both describe "technical breakout AND sentiment," but no breakout signal existed anywhere in the pipeline — `propose_order()` only ever checked sentiment. Added `app/quant/technical_signals.py` (deterministic SMA-breakout + volume-confirmation, matching the "quant is LLM-free" design principle) and wired it through `quant_engine.py` → `state["technical_signal"]` → Track 1's entry check, now a real AND-gate.
    - **Track 1's stop-loss would have rejected every order**: strategy set `STOP_LOSS_PCT = 0.30`, but `risk_gate.py` rejects anything above the global ceiling (`settings.stop_loss_pct`, default 0.20) — a live blocker, found while writing the track doc's exit-management section, not from a test run. Fixed by tightening the strategy to 0.20 rather than loosening the platform-wide ceiling.
    - **Track 3 had the same `market_data` shape bug already fixed in `mcp_client.py`**: `state.get("market_data", {}).get("close")` assumed a dict; `market_data` is actually a list of bar records (fixed the `AgentState` type hint too, it was wrong). Never triggered live since Track 3 hasn't run yet, but would have crashed identically to the MCP bug the moment it did.
    - All re-verified: fresh Python import check across every edited module, then one more full live cycle end-to-end (real Alpaca + real Featherless/Kimi K2) confirming the corrected thesis and no crashes.
  - Wrote `docs/tracks/00_options_basics.md`, `track1_alpha_spreads.md`, `track4_income_wheel.md` — options mechanics grounded correctly (intrinsic vs. extrinsic value, Delta ≠ 1:1 immediate price sensitivity — the pasted external explainer's math was a valid *at-expiration* example but read as instantaneous, which isn't right pre-expiration), the actual entry/exit rules matching the real code exactly, the LLM-explains/code-decides architecture split, and an honest tone (advised against "revolutionary"/"bound to be profitable" framing — no options structure is bound to be profitable; the real, defensible claim is the bounded risk + deterministic risk gate, which is demonstrable in code).

- **2026-08-24 (session 4)** — Planned (via a formal Explore → Plan → user-approved plan cycle) and implemented multi-symbol scanning plus closing Track 1/4's remaining gaps. Track 4 (Income/Wheel) confirmed as the committed secondary track; Track 2 stays a conditional third, scoped to the Sep 4 jobs-report week if pursued (section 5).
  - **Research before writing any code found something more fundamental than the documented gaps: nothing in the pipeline could place a real order at all, regardless of track.** `_common.py`'s `closest_by_delta()` filtered on `c.get("type")`, but the old `get_option_chain()` returned raw `OptionsSnapshot`s which have no `type`/`strike_price`/`expiration_date`/flat `bid`/`ask` fields at all — the filter could never match, so it always returned `None`. Neither of session 3's live tests caught this because sentiment never cleared the entry threshold either time — the code never reached the broken path. `execution.py` had a second unreachable bug directly downstream: it called `place_option_order(order)` with one dict, but the real function takes separate `symbol`/`legs`/... args with `legs` required.
  - **Rebuilt `rest_client.py`'s `get_option_chain()`** to join two separate Alpaca APIs by OCC symbol: `TradingClient.get_option_contracts()` (type/strike/expiration/open_interest — a different endpoint than before) with the existing market-data snapshot (bid/ask/greeks/IV). **Verified against real live data, not just unit tests**: 5,652 real SPY contracts returned with correct fields; found and fixed a second latent bug this exposed (`c.get("greeks", {})` doesn't help when the real value is explicitly `None`, not absent — Alpaca's paper feed returns `None` greeks for a real fraction of contracts, especially 0 DTE ones).
  - **Extended `closest_by_delta()`** with DTE-awareness and confirmed a live correctness bug building a real Track 1 order for the first time ever: without `target_dte`, the two legs of a "vertical" spread could land on *different expirations* (got a real result buying an October call against selling a same-day August call) — not a valid spread at all. Fixed by passing `target_dte` through both legs' selection, with a same-expiration assertion as a second layer of defense. Also fixed Track 1's put-delta sign bug (unsigned deltas rank backwards against puts' negative range) while adding the bear put spread branch — confirmed live: both a bullish call spread and bearish put spread now build correctly with matched expirations and sensible strikes.
  - **Added `position_monitor.py`** (a plain function per the user's explicit choice over a LangGraph node — it's an account-wide sweep, not a per-symbol decision, and needs no LLM): closes Track 1 spreads at 50% profit-take / 20% stop-loss against the real net debit paid (added `PROFIT_TAKE_PCT` — referenced in prose since session 1 but never actually defined in code until now), and syncs Track 4's `holds_underlying_shares` from Alpaca's real position list each sweep (previously hardcoded unreachable-`False`). Wired into `run_agent_loop.py`, running once per full pass before new entries are proposed. `Trade` gained `status`/`closed_at`/`realized_pnl` columns; `WheelState` is a new tiny table.
  - **Closed Track 4's gaps**: an IV-percentile floor (50, a concrete reading of the brief's "elevated IV"), DTE targeting (21-day point estimate for the 14-30 range), and the `holds_underlying_shares` fix above.
  - **Found and fixed a capital-sizing bug** while wiring risk_gate.py to use net debit/credit instead of gross notional: a cash-secured put's true capital commitment is the strike price x 100 (cash secured against assignment), not the small premium collected — using premium alone would let the position-size cap pass almost any CSP regardless of strike. Added a `capital_at_risk` field each strategy sets explicitly (debit spread's net debit; CSP's strike x 100; covered call's $0, since the shares are already owned) rather than one formula trying to fit every trade shape.
  - **Added the three new risk-gate guardrails** (Balanced posture, chosen with the user over Conservative/Aggressive): max 3% of equity per trade (down from 10%), max 6 concurrent positions, 55% cash-reserve floor, 15% sector cap. All four rejection paths (position size, concurrent count, cash reserve, sector) directly unit-tested against `risk_gate.run()` with fabricated states — each fires with the correct, specific rejection message.
  - **Added `app/watchlist.py`** — the user's own researched 15-symbol curated universe (index ETFs, mega-cap tech, high-beta momentum, sector ETFs), doubling as the sector map for the concentration cap, plus a liquidity pre-filter (20-day avg daily volume > 1M, ATM bid-ask spread < 5%) using a new `get_daily_bars()` (the existing `get_recent_bars()` is minute-bars only). A "Load Curated Watchlist" button in the Controls tab populates the Symbols field from it via a new `/api/config/watchlist` endpoint. Deliberately NOT a full-market scan — the loop sleeps once per full pass over all symbols, not per symbol, so a much larger list would silently stretch the effective per-symbol interval.
  - **A real operational lesson mid-session**: a "no such column: trades.status" error on the first post-schema-change test turned out to be a stale `deltamind.db` whose deletion had silently failed (`-ErrorAction SilentlyContinue` masked a file-lock from an old backend process still holding it open) — `create_all()` only creates missing tables, never alters existing ones. Fixed by finding and killing the actual lock-holding process (a stray system-Python uvicorn instance from an earlier restart, not the venv one) before deleting, then verifying the fresh schema directly via `sqlite3` before re-testing.
  - **Re-verified the entire pipeline end-to-end after all changes**: full pytest suite (11/11, including new tests in `tests/test_strategies_common.py` for the DTE/sign-fix logic and `net_debit_credit`), clean `tsc -b`, and one more full live cycle (real Alpaca + real Featherless) — position monitor sweep ran with zero errors against the fresh schema, two real LLM calls succeeded, and the persisted decision showed a correct, track-consistent thesis.

- **2026-08-24 (session 5)** — A live overnight test cycle surfaced a real `RemoteDisconnected` error mid-session (a stale pooled HTTP connection to Alpaca's data API — fixed reactively with a one-retry wrapper in `rest_client.py`, `_retry_once_on_disconnect`, before this session's main planned work). Main goal: get both tracks running reliably, continuously, unattended for a 3-day local test window (through Fri) before the hackathon, with clear per-track LLM prompts and a way to see each track's performance separately. Planned via a formal Explore (3 parallel agents) → Plan agent → 6 AskUserQuestion clarifications → user-approved plan cycle.
  - **Rate limiting (previously nonexistent)**: research confirmed zero protection existed — no interval floor (0/negative accepted), no LLM call budget, no circuit breaker on repeated failures, and Alpaca SDK's own retry only covers HTTP 429s, not the connection-drop class of error just hit live. Added `app/rate_limits.py` (shared constants: 60s interval floor, 300/hr and 2000/day LLM call caps, 3-consecutive-failed-pass circuit breaker), `app/llm/rate_limiter.py` (in-memory rolling-window budget), and a manual 3-attempt retry in `llm/client.py` catching the SDKs' typed rate-limit/connection exceptions specifically. `run_agent_loop.py` now exits with a distinct code (3) when the circuit breaker trips, rather than retrying a dead credential forever.
  - **Local reliability (previously nonexistent)**: `agent_loop_manager.py` gained a daemon watchdog thread that auto-restarts the trading-loop subprocess on an unexpected crash (backoff: 5/15/30/60/120s, up to 20 attempts) but deliberately does NOT auto-restart past the circuit-breaker exit code — restarting into the same persistent failure would defeat the breaker's purpose. The backend itself now auto-resumes trading after its own restart (a real behavior change from the previous explicit-Start-only model, confirmed with the user) via a small `.engine_run_state.json` flag file. `run_agent_loop.py` also now calls Windows' `SetThreadExecutionState` to block inactivity sleep while running. **Verified live, not just written**: force-killed the subprocess mid-run and confirmed the watchdog detected it and relaunched with a new PID within the poll window, `auto_restart_count`/`last_crash_reason` correctly surfaced via `/api/engine/status`. Full Windows-reboot coverage (Task Scheduler) was explicitly scoped out per the user's choice — in-process crash recovery only for this window; see `docs/local_reliability.md`.
  - **Track 1 thresholds were never once clearing in real testing** (±0.75 sentiment, 1.5x volume — confirmed via research, not assumption). Moderate-loosened to ±0.5 sentiment / 1.2x volume as new defaults, and — per the user's explicit ask — made **runtime-configurable via the Controls UI** (two new fields) rather than hardcoded, threaded through `AgentState` → `graph.run_cycle()` → `technical_signals.detect_breakout()` / `track1_alpha_spreads.propose_order()`, all the way out to the CLI args and `StartRequest`'s Pydantic validation (also where the rate-limiting interval floor landed). A config change requires Restart, same as symbols/track today — deliberately not a new "read config from disk inside the subprocess" mechanism, to keep the existing argv-based architecture consistent.
  - **Fixed a prompt-drift bug found while making the thresholds configurable**: `lead_architect.py`'s Track 1 prompt text hardcoded "±0.75" — once the real gate became tunable, that text would silently drift from the actual threshold being checked the moment someone changed it in Controls (same bug class as session 3's track-drift fix). Fixed by making `TRACK_ENTRY_RULES["track1_alpha_spreads"]` a callable that interpolates the live threshold values from state each cycle, instead of a static string. Also rewrote Track 4's prompt entry, which was missing its actual IV-percentile gate and a "no trade" phrasing pattern that Track 1's already had.
  - **Track P&L separation**: `list_trades`/`list_agent_decisions` gained an optional `track` filter; new `get_track_pnl_summary()` (realized P&L, win rate, open/closed counts, aggregated in Python — small dataset, avoids a dialect-specific conditional-sum) and a new `pnl_utils.compute_unrealized_pnl()` (deliberately a standalone helper, not a refactor of the already-live-verified `position_monitor.py` exit logic — not worth the regression risk on tested trading code for a read-only dashboard number). New `/api/trades/pnl-summary` endpoint; Controls page gained a Track P&L card row and a track filter on Decision History, per the user's explicit choice of extending Controls over a new dedicated tab.
  - **Found, not fixed this session**: Track 4 has no realized-P&L exit path at all today — `position_monitor.py` only ever calls `close_trade()` for Track 1, so Track 4 trades never leave `status="open"`, meaning Track 4's realized P&L will read $0 even after a real assignment/expiration cycle during the test week. Flagged in section 11, not blocking.
  - **Cloud deploy prep** (Render, explicitly "prepare, don't cut over") — `backend/Dockerfile`, `render.yaml`, `docs/deployment.md` — run as an independent background task in parallel with the rest of this session's work since it touches no shared files; frontend deploys as a separate static site per the user's confirmed choice, not mounted under FastAPI.
  - **Re-verified after every step, not just at the end**: full pytest suite (11/11) after each of steps 1-5; `tsc -b` clean; a live smoke test against a real running backend on a scratch port — start/stop lifecycle, the new Pydantic 422 on a sub-60s interval, `/api/trades/pnl-summary` returning correctly-shaped empty summaries, and the watchdog auto-restart test above — before cleaning up the test server.

  - **Same-day addendum: found and closed a real architecture gap before actually going live** — the whole point of this session was "run both tracks continuously to compare them," but `agent_loop_manager.py` was still a single-process singleton; there was no way to run Track 1 and Track 4 at once. Fixed by making it one `AgentLoopManager` instance per track (`get_manager(track)`, lazily created, own watchdog thread, own log file `logs/agent_loop_{track}.log`, own run-state file for auto-resume), and threading `track` as a routing param through `routes_engine.py`/`routes_logs.py`/`config_store.py`'s run-state functions/`main.py`'s startup auto-resume loop. Also added SQLite WAL mode + a 30s busy-timeout (`db/session.py`) — running two independent OS processes writing to the same `deltamind.db` file would otherwise risk "database is locked" errors, previously a non-issue with only one loop process. Frontend `ControlsPage.tsx` now shows two independent engine panels (own status/uptime/cycle counts/Start-Stop-Restart each), sharing the Symbols/Interval scan settings; `LogsPage.tsx` gained a track selector.
  - **Wrote `docs/tracks/technical_pipeline.md`** — full code-accurate technical reference (exact prompts, exact data fetched, exact LLM call mechanics) per the user's request to understand the pipeline at the implementation level. Found and documented several honest gaps while writing it: the Controls UI's "Temperature" field is not wired to any LLM call (dead setting); the only technical indicator is the SMA-breakout-plus-volume signal (no RSI/MACD/etc.); Greeks shown to the LLM are Alpaca's own, not derived from this repo's own (unit-tested but otherwise unused) Black-Scholes module; `iv_percentile` is calibrated against a hardcoded 52-week IV placeholder range; Track 4 pays for a news/sentiment LLM call every cycle that its own entry logic never reads; the LLM rate-limit budget is per-process, not shared across the two concurrently-running tracks; `news_analyst.py` calls `mcp_client.get_news` with a bare string where the signature expects a list (works in practice, unverified against the MCP server's own validation).
  - **Caught a real bug within minutes of actually going live**: Track 4's very first SPY cycle crashed with `json.JSONDecodeError` — Featherless returned an empty body for the sentiment call, and `structured_generate()` had no fallback for a successful-HTTP/malformed-content response (only `generate()`'s retry covered connection-level failures). Fixed by giving `structured_generate()` its own retry loop that requests a fresh completion (not just re-parsing the same bad string) up to 3 times before raising a clear error. Both engines restarted to pick up the fix.
  - **Both tracks started for real** against the full 15-symbol watchlist, 300s interval, real Alpaca paper account + real Featherless — confirmed independently running via `/api/engine/status?track=...` (distinct PIDs), Track 1's first cycles correctly REJECTED (no qualifying setup yet, working as designed).
  - **User correction, same day: the combined dual-track dashboard UI was wrong.** Only the *backend's* ability to run both tracks concurrently was the actual goal — the submission ships with exactly ONE track running, and the UI should never show or compare tracks at all, not even during testing. Reverted `ControlsPage.tsx`/`LogsPage.tsx`/`StrategiesPage.tsx`/`App.tsx` back to single-engine layouts (closer to session 4's original design). Backend stays exactly as built (per-track `agent_loop_manager.py`, unchanged) — it already treats `track` as a routing parameter for everything, so no backend changes were needed to fix this, only frontend. Added `frontend/src/config.ts`'s `ACTIVE_TRACK` constant, sourced from a `VITE_ACTIVE_TRACK` build-time env var (`.env.track1`/`.env.track4`, wired to new `npm run dev:track1`/`dev:track4`/`build:track1`/`build:track4` scripts) — an entire running frontend instance is locked to one track with zero UI surface mentioning the other, so two instances can run side-by-side for testing (different ports) without ever looking like a comparison tool, and the exact same build mechanism produces the actual single-track submission artifact later with no last-minute stripping.
  - **User also flagged a real strategy issue surfaced by the (since-reverted) combined Decision History view**: several Track 4 cash-secured-put proposals on expensive names (TSLA, META) were rejected for position size 34-53% (vs. the 3% cap) — correctly caught by the risk gate, no capital ever at risk, but a real inefficiency: a CSP's capital commitment is strike×100, so expensive underlyings are structurally guaranteed to fail the 3% cap every time, wasting a full LLM cycle (2 calls) proposing something that can never pass. **Not fixed yet** — flagged for a future pass (e.g. skip CSP candidates above an affordable-strike threshold before calling the LLM at all).
  - **Also fixed live**: `llm/client.py`'s `structured_generate()` had no fallback for a successful HTTP call that returns an empty/non-JSON body — hit for real within minutes of Track 4 going live (Featherless returned an empty string for a sentiment call, crashing that cycle). Added a proper retry (fresh completion request, not re-parsing the same bad string) before raising.
  - **End state at session close: both engines stopped** at the user's request, pending review of the corrected single-track UI before restarting.

- **2026-08-26 (session 6)** — Split Track 1's and Track 4's Controls pages into two tabs within one running frontend instance ("Track 1"/"Track 4", each hardcoded via a `track` prop on `ControlsPage` rather than the env-var lock) so both can be tested side-by-side without two dev servers — the env-var (`ACTIVE_TRACK`) mechanism from session 5 stays for the eventual single-track submission build. Several UI refinements followed (Provider/Model side-by-side, API Key + Test button merged into one row, Temperature input removed as dead/hardcoded, Performance section moved to fill freed grid space, a searchable multi-select `SymbolPicker` replacing the plain Symbols text field + separate "Load Curated Watchlist" button, and a latent `Select` primitive bug fixed — the dropdown arrow was positioned against the wrong element's width, floating detached from short option text and overlapping long option text).
  - **Track 1's full strategy redesign** — the main event this session. The user supplied a complete engineering spec (technical confluence screener gating the LLM call, single-leg 0-1 DTE naked options, a merged LLM catalyst validator, tiered exit management, real bar-close-triggered execution) after noticing Track 1 never had a deliberately-designed entry gate (the SMA-breakout-plus-volume signal was a documented placeholder from session 4). Planned via a formal Explore → 3 AskUserQuestion clarifications → user-approved plan cycle, then one more mid-plan user refinement (1-2 DTE instead of 0-1 DTE, with recalibrated exit numbers) before implementation.
    - **Three decisions locked in with the user, all deliberately deviating from the literal spec**: position sizing stays at the existing 3% cap (not the spec's 2%/3.5%) — "use the 3 percent still"; the LLM gate (`|sentiment| >= 0.50 AND confidence >= 70%`) is hardcoded, not a new Controls-UI-tunable field (the old `sentiment_threshold`/`volume_ratio_min` fields and their entire plumbing were removed for Track 1 only — Track 4's are untouched); and bar-close triggering is a **real Alpaca websocket stream** (`alpaca.data.live.StockDataStream`, confirmed installed and working), not a fast-poll approximation — the user explicitly chose the higher-effort, more correct option over the pragmatic fallback offered.
    - **`quant_engine.py` branches by track**: Track 1 calls a new `check_track1_confluence()` (15m EMA(50) trend + 1m VWAP + RSI(14) band + 20-bar breakout + RVOL >= 1.5, all agreeing on direction) in `technical_signals.py`; every other track still calls the original `detect_breakout()`, byte-identical to before — zero Track 4 behavior change. `graph.py` gained a conditional edge after `quant_engine` that sends an unqualified Track 1 cycle straight to `END` (**zero LLM calls** — this early-exit-before-LLM path did not exist for any track before this session) and a qualified one to a new `track1_validator.py` node instead of `news_analyst.py`/`lead_architect.py` — one merged LLM call (verdict/sentiment/confidence/TP/SL/hold-time schema) replacing the old two-call sentiment-then-thesis split, for Track 1 only; `lead_architect.py`'s dead Track 1 entry was deleted, tracks 2/3/4 unchanged.
    - **Deliberate deviations from the doc, each resolving a real conflict with existing code rather than guessing**: the 15m EMA is fetched as **native 15-minute bars via a new `rest_client.get_15m_bars()`**, not resampled from the 1-minute buffer — 500 1-minute bars only resample to ~33 complete 15-minute candles, short of the 50 needed to seed a stable EMA. Stop-loss clamps to the *existing* 20% platform ceiling (`risk_gate.py`'s `settings.stop_loss_pct`), not the doc's original 25% (later recalibrated to 20% by the user's own 1-2 DTE refinement anyway, so the two now agree with zero deviation). Contract sizing uses total premium × qty directly rather than `_common.net_debit_credit()` — that helper assumes `ratio_qty` is the true per-order multiplier (true for every prior single-unit strategy), which breaks once Track 1 became the first strategy to size `qty > 1` per trade; fixed by keeping each leg's `ratio_qty=1` (matching Alpaca's real single-leg order semantics) and sizing `capital_at_risk` explicitly instead. `execution.py`'s hardcoded `qty=1` was also a real latent bug this exposed — fixed to read `order.get("qty", 1)`.
    - **`track1_alpha_spreads.py` rewritten**: single-leg (not 2-leg spread) selection via the existing `closest_by_delta(target_dte=2)`, with explicit guards rejecting anything outside 1-2 DTE or a 0.45-0.55 delta band (closest-by-delta can't itself express a 2-expiration-wide window); dynamic qty sized to 3% of equity; fixed two-tier exits (`tp1_pct=0.50` partial, `tp2_pct=1.00` runner — not derived from the LLM's own take-profit suggestion); `max_hold_minutes` clamped to ≤120 (default 90, extended from the spec's original 45 per the DTE recalibration).
    - **`position_monitor.py`'s Track 1 sweep rewritten** for single-leg P&L (reads Alpaca's own `unrealized_plpc` directly — simpler than the old net-debit-spread math) with real new automated exit logic: partial close at +50% (moving the stop to breakeven for the remainder), full exit at +100% or a 15m EMA(50) reversal against the trade, hard stop-loss, a 90-minute time-stop for a flat/stalled position, and an unconditional 3:45pm ET EOD liquidation. Needed one new DB column (`Trade.tp1_triggered`) — since `Base.metadata.create_all()` doesn't alter existing tables, applied a one-time non-destructive `ALTER TABLE` to the local `deltamind.db` rather than deleting the user's existing test data.
    - **New `scripts/run_agent_stream_track1.py`** — Track 4 keeps `run_agent_loop.py`'s interval-polling loop completely unchanged; `agent_loop_manager.py`'s `_spawn()` gained one track-conditional branch to launch this new script instead, with only `--symbols` (no `--interval`/threshold flags at all). Runs `StockDataStream.run()` in a background daemon thread (its own internal event loop) with the position-exit sweep as a plain synchronous loop on the main thread — deliberately NOT one shared asyncio loop juggling both concerns, since `mcp_client.py`'s `_run()` calls `asyncio.run(...)` for every Alpaca trading/news call, which would raise if invoked from the stream's own already-running loop; the bar-close handler instead dispatches the (synchronous) per-symbol pipeline via `asyncio.to_thread(...)`, which has no running loop of its own. Reuses the same circuit-breaker exit code as `run_agent_loop.py` so `agent_loop_manager.py`'s watchdog needs no changes to understand it.
    - **Frontend**: `ControlsPage.tsx`'s field-visibility gate flipped from "show thresholds only for Track 1" to "show Interval + thresholds only for Track 4" — Track 1's page now shows none of them, matching the new hardcoded-gate/streamed-triggering design; `StrategiesPage.tsx`'s Track 1 param table and `types.ts`'s summary rewritten to describe the new strategy.
    - **Same-day fix from a user question about the streaming design**: `run_agent_stream_track1.py` originally only screened a symbol on its first *natural* bar-close event — up to ~60s of dead time right after connecting where nothing runs at all, even though the full historical window is already available via REST. Added an immediate warmup pass (screens every symbol once right after the stream connects, using the same code path as a bar-close trigger) so the strategy is live from the moment it starts. Confirmed with the user's own question that this is safe from a "partial/still-forming candle" problem: there's no incrementally-built buffer of our own to get confused (every cycle re-fetches its own window fresh via REST, which by construction only returns already-elapsed bars), and Alpaca's `bars` websocket channel (used purely as the trigger) only fires once a minute has genuinely closed — not a live-updating tick stream the way `quotes`/`trades` are.
    - **A real gap the user caught before live-testing began**: the confluence screener only checked the 15m EMA(50) trend, not the 1-minute chart's own trend — a symbol could qualify on a 15m uptrend while its immediate 1-minute trend actually disagreed. Added a 1m EMA(20) requirement (period chosen by the user over EMA(50)/EMA(9), matching the existing 20-bar breakout/RVOL lookback already used elsewhere in the same screener) that must independently agree with the 15m EMA for either direction to qualify — both timeframes now required, not just the higher one.
    - **Verification**: 25 new unit tests (`test_technical_signals_track1.py`, `test_track1_alpha_spreads.py` — confluence math confirmed by running the real functions against synthetic bar data rather than hand-derived expected values; contract selection/sizing/SL-clamp/DTE-and-delta-band-rejection all covered), full `pytest` suite green (36/36, up from 11), `tsc -b` and `npm run build` both clean, and the rewired LangGraph confirmed to compile with the new conditional routing and `track1_validator` node. **Not yet exercised against a real live market cycle** — unit-tested and import-verified only; both `docs/tracks/track1_alpha_spreads.md` and `docs/tracks/technical_pipeline.md` rewritten to match (exact new prompt/schema/data pipeline/worked example), old content preserved in a short history section rather than deleted.
  - **Same day: Track 1 went live for real** — backend started, Alpaca paper connection confirmed (account PA31AXNLB6OS, $100k equity), `run_agent_stream_track1.py` started against the full 15-symbol watchlist, websocket connected to `wss://stream.data.alpaca.markets/v2/iex`, warmup pass + live bar-close events both firing correctly with zero LLM calls burned on any unqualified symbol — confirming the redesign's core efficiency claim for real, not just in tests. Stopped again at the user's request once it became clear the market was at/past today's close (~4:53pm ET) rather than leave an autonomous trader connected unsupervised overnight with nothing to actually screen. A concurrency question from the user during this ("are symbols checked simultaneously, does a new candle take as long as the ~2min warmup pass looked like") was answered from direct code inspection, not assumption: the warmup pass is genuinely sequential (one-time startup cost only), but live bar-close events dispatch via `asyncio.to_thread` per symbol and run concurrently, not queued — the ~2min spread in the pasted logs was actually two separate minutes' bar-close batches interleaving with the tail of the warmup pass, not one batch stuck processing. Found (not fixed, flagged as low-priority) a real minor issue while answering this: `llm/rate_limiter.py`'s `_RollingWindowLimiter.check_and_record()` mutates a shared list with no lock, a small race risk now that multiple symbols' cycles can hit the LLM gate in the same instant — worst case is an undercounted budget, not a risk-limit bypass.

- **2026-08-26 (session 7)** — Track 4 streamlining, mirroring session 6's Track 1 redesign process (research existing code → present findings/tradeoffs → `AskUserQuestion` scoping decisions → formal plan → user-approved → implement). Prompted by a user-supplied engineering spec for a real Wheel strategy (IV percentile + daily technical regime entry, a merged LLM risk-officer validator, tiered profit-target/stop-loss exit management, cost-basis-aware covered calls) after noticing Track 4, like Track 1 before session 6, never had real exit management or a technical filter — it only ever opened positions.
  - **A blocking bug found during research, not in the spec**: `risk_gate.py` applied the same 3% `max_position_pct` cap (calibrated for Track 1's premium-based risk) to Track 4's cash-secured-put collateral (strike×100, correctly set as `capital_at_risk` since session 4). Pulled real prices for the whole watchlist to check the math — **every single symbol failed, including the cheapest** (XLF at $58/share is already 5.8% collateral on a $100k account; SPY at $765/share is 76.5%). Track 4 as wired going into this session could not have placed one single trade on anything.
  - **Three decisions locked in with the user before implementing**: (1) a new, separate `max_wheel_collateral_pct` setting (25% per position) rather than raising the shared cap — collateral posted for a CSP isn't capital-at-risk the way Track 1's premium is, so it doesn't belong under the same ceiling; Track 1's 3% cap is untouched. (2) Keep the existing shared 15-symbol watchlist rather than building a separate cheaper one — the user's own choice, after a follow-up question ("can I just use a separate paper account?") surfaced that a genuinely separate account requires threading per-track credentials through `rest_client.py`'s cached clients and `mcp_client.py`'s session launch (real scope, not a toggle) and, more importantly, doesn't even fix the underlying math unless *also* funded at a larger balance — so the user chose to just fund the existing shared account higher (~$500k target) instead, a zero-code-change lever. (3) Replace the permanent hardcoded `(0.10, 0.90)` IV range with a real self-bootstrapping history (new `iv_observations` table, populated every cycle for every track unconditionally since `compute_iv_percentile` already ran that way) rather than leaving the placeholder — falls back to the old range below 20 samples, real data above it.
  - **Also surfaced, not fixed this session**: Track 1 and Track 4 currently share one Alpaca account entirely (`portfolio_risk_snapshot()` has zero track filtering) — `max_open_positions`/cash-reserve/margin checks are combined across both tracks, not per-track. Out of scope given the account-balance-bump decision above.
  - **`risk_gate.py`** now branches its position-size cap by `state["track"]` — `max_wheel_collateral_pct` for Track 4, `max_position_pct` for everything else — the one-line fix that actually unblocks Track 4 trading.
  - **New daily-bar regime gate** (`technical_signals.check_wheel_put_regime`: price > 200-day EMA AND RSI(14) in [35,55]) gates fresh cash-secured-put entries only, per the spec — covered calls don't need it, only a cost-basis floor. `quant_engine.py`'s track branch became 3-way (Track 1 → confluence, Track 4 → daily regime via a new `get_daily_bars(limit=220)` call, tracks 2/3 → unchanged `detect_breakout`).
  - **`track4_income_wheel.py` rewritten**: liquidity pre-filter (OI≥500, spread≤5%) applied at contract-selection time for the first time (previously only a one-time watchlist-curation check); explicit DTE `[14,30]` and delta `[0.25,0.30]` band rejection guards (same pattern as Track 1's DTE/delta bands); covered-call candidates filtered to `strike_price >= cost_basis` before ranking by delta, degrading gracefully (no floor) if `cost_basis` isn't recorded; `strike_price` added directly to the leg dict so `position_monitor.py` never needs to parse an OCC symbol; IV floor tightened from the scaffold's 50 to the spec's 45; `propose_order(state, llm_result)` signature changed from `(state, thesis)` to match Track 1's validator-driven pattern.
  - **New `track4_validator.py`** — merged LLM "Lead Risk Officer" call (verdict/risk_score/earnings_conflict/confidence_pct/profit_target_pct/thesis schema, spec's prompt verbatim), reached only when the deterministic gate above already qualifies (same zero-cost-when-unqualified principle as Track 1's redesign — Track 4's `lead_architect.py`/`news_analyst.py` path fired unconditionally every cycle before this, regardless of IV percentile). Hardcoded gate: `APPROVE AND NOT earnings_conflict AND risk_score <= 0.35 AND confidence_pct >= 70`. Confirmed via a direct check of this codebase and Alpaca's MCP tools that no real earnings-calendar data source exists anywhere — the "earnings within 21 days" check is necessarily an LLM judgment from news headlines, documented plainly rather than oversold. Correctly calls `get_news([symbol])` as a list — `lead_architect.py`'s `track4_income_wheel` entry removed as dead code, same cleanup Track 1 got in session 6.
  - **`position_monitor.py`'s Track 4 sweep**: previously `_sync_wheel_state()` only ever flipped a `holds_underlying_shares` boolean and never closed a single trade. Replaced with a full sweep: profit-target buy-to-close at +50% of collected premium (fixed constant, not LLM-tunable, same split as Track 1's TP/SL); stop-loss defense (cost to close reaches 3x original premium AND the daily 200-EMA regime has broken — two different mechanisms from the order-level static `stop_loss_pct` risk_gate checks at entry, deliberately not conflated); and, since an option position can vanish between sweeps three genuinely different ways, all three are distinguished and handled: assignment (records `cost_basis = strike - premium`, flips `WheelState.holds_shares`), called-away (realizes premium + capital gain, clears `WheelState`), and plain expiration worthless (closes the trade, keeps 100% premium, state unchanged) — the last being the most common outcome per the spec's own 70-85% win-rate estimate, and the case the initial plan sketch under-specified until writing the real logic surfaced it.
  - **New `WheelState.cost_basis` column** (nullable, non-destructive `ALTER TABLE` applied to the local `deltamind.db`, same pattern as session 6's `Trade.tp1_triggered`) and new `IVObservation` table (auto-created by `create_all()`, no migration needed) plus `record_iv_observation`/`get_iv_52wk_range` rewritten in `repository.py`.
  - **Frontend**: Track 4's Sentiment Threshold/Volume Ratio Min fields hidden in Controls (both tracks now hardcode their LLM gate, so both fields are vestigial everywhere, not just Track 1) — Track 4 keeps only the Interval field, still genuinely interval-polled unlike Track 1's streaming. `StrategiesPage.tsx`'s Track 4 param table and `types.ts`'s summary rewritten to describe the real mechanics (regime gate, delta/DTE bands, wheel collateral cap, risk-officer gate, profit-target/stop-loss-defense numbers).
  - **Verification**: 23 new unit tests across four new files (`test_technical_signals_wheel.py`, `test_iv_percentile.py`, `test_track4_income_wheel.py`, `test_risk_gate.py` — the regime-qualifying test's RSI-midband synthetic data was verified by actually running the function against candidate values rather than hand-derived, same lesson learned in session 6's confluence tests) plus a new `tests/conftest.py` (this project has no test-DB fixture; the new `iv_observations` table needed `Base.metadata.create_all()` run once before any DB-touching test, which nothing previously required). Full `pytest` suite green (60/60, up from 37), `tsc -b` clean, LangGraph confirmed to compile with the new `track4_validator` node. `position_monitor.py`'s new Track 4 sweep has no mocked unit tests, matching this project's existing precedent (Track 1's sweep doesn't either — this test suite covers pure deterministic functions, not logic requiring mocked live Alpaca position state) — reviewed and internally consistent, not yet exercised against a real fill/assignment/expiration.

- **2026-08-27 (session 8)** — Three focused follow-ups before resuming live testing, each user-initiated mid-conversation rather than planned in advance.
  - **Per-track Alpaca account separation.** The user, trying to raise the paper account's balance, independently realized hackathon submissions must score each track separately — Track 1 and Track 4 can't share one account's fills/P&L at submission time, needing two. Implemented the plumbing with zero behavior change until acted on: `config.py` gained `alpaca_api_key_track4`/`alpaca_secret_key_track4` (blank by default) and a `get_alpaca_credentials(track)` resolver falling back to the shared pair; `agent_loop_manager.py`'s `_spawn()` passes the resolved pair as that subprocess's own env — simpler than it first looked, since each track already runs as a fully separate OS process (from the Track 1 websocket redesign), so there's no shared client cache to fight. `rest_client.test_connection()`/`/api/config/test-alpaca` now take an optional per-track credential pair; the Controls UI's "Test Alpaca Connection" button tests whichever track's tab it's clicked from. `.env.example`, `backend/.env`, `docs/deployment.md` updated for the Day-1 two-account checklist. Separately noted: the user couldn't find Alpaca's paper-balance-reset control in their dashboard — unresolved, worth revisiting once the two new accounts exist, since SPY/QQQ-class CSP collateral (session 7) only fits comfortably under the 25% wheel cap with a larger-than-$100k balance.
  - **Latency instrumentation + optimization, chasing a user-stated <2s candle-close-to-decision target for Track 1.** Added `graph.py`'s `_timed()` wrapper (every node logs its own ms to the same log file the WAIT/TRADE line goes to) plus an outer wall-clock timer in `run_agent_stream_track1.py` measured from the literal websocket bar-close event (the real zero point, including asyncio dispatch overhead) — LATENCY_BUDGET_MS=2000, logged as a WARNING when exceeded. First real cycle timed at ~13,047ms, almost entirely `market_ingestion`'s unconditional option-chain fetch (thousands of contracts for a liquid underlying) — confirmed via the new per-node breakdown, not guessed. Fixed in two rounds, both requested by the user ("do the change" / "I prefer things to run in parallel where they can"): (1) deferred Track 1's option-chain fetch out of `market_ingestion.py` entirely, into `track1_validator.py`, so it's only ever paid on a cycle that's already cleared both the technical gate and the LLM catalyst check — cut market_ingestion from ~11,937ms to ~1,562ms; (2) `market_ingestion.py` became the graph's *only* I/O node (moved Track 1's `get_15m_bars` and Track 4's `get_daily_bars` out of `quant_engine.py`, which is now genuinely pure computation matching its own docstring) and fetches each track's needed combination concurrently via one `ThreadPoolExecutor`; `risk_metrics.portfolio_risk_snapshot()`'s two independent calls (`get_account_info`/`get_all_positions`) got the same treatment in their own small pool. Net: a WAIT cycle's steady-state (post-cold-start) latency dropped from ~13,000ms to ~500-600ms — comfortably under budget; a qualifying cycle still adds real, now individually-visible time for `track1_validator` (LLM + the deferred chain fetch) and `execution` (the actual Alpaca order placement), left alone since a full LLM-driven decision can't itself be parallelized away. `AgentState` gained `stage_timings_ms`/`bars_15m`/`daily_bars` fields; 71 tests stayed green throughout (60→71, four new latency/graph-routing tests folded into the count below).
  - **Deterministic red-folder news blackout gate**, per the user's own description of the mechanism they wanted: no new entries within 5 minutes before/after a scheduled high-impact macro release, the full day's macro calendar (past and upcoming) surfaced to the LLM validators for context, and all timestamps standardized (not left to whatever timezone confusion the user, reading logs from Kenya, might otherwise hit). New `app/quant/news_calendar.py`: `RED_FOLDER_CALENDAR` is a manually curated, week-specific list (no free/already-integrated live economic-calendar API exists in this project — same category of gap as Track 4's earnings-conflict check) — seeded via web search against federalreserve.gov (confirmed no FOMC meeting Aug 28-Sep 4, 2026; next is Sep 15-16), ismworld.org, bls.gov-adjacent sources, and adp.com, each entry's `source` field noting where it was verified rather than presenting invented dates as fact. All comparison happens on explicit UTC-aware datetimes (never the host clock); display/logging renders both UTC and `Africa/Nairobi` (EAT, UTC+3, no DST) side by side. New `news_blackout_gate.py` node is the graph's entry point for every track (before `market_ingestion` even runs — a blocked cycle costs zero REST/LLM calls, same principle as Track 1/4's own screens); `risk_gate.py` re-checks the same condition as a backstop for a cycle that started clear but drifted across the boundary during a slow LLM call. Deliberately does NOT gate `position_monitor.py`'s exit sweep — a stop-loss or EOD liquidation should still fire during a news spike; only new entries are blocked. `track1_validator.py`/`track4_validator.py`'s prompts both gained `todays_calendar_summary()` (separate from per-symbol headlines) so the catalyst/risk-officer LLM can relate a price move to a release that already happened or one still to come, per the user's explicit ask. 11 new tests (`test_news_calendar.py`'s boundary-inclusive blackout-window math, `test_graph_news_blackout.py` confirming the gate actually short-circuits before any node runs, not just that a field gets set) — full suite now 71/71, `tsc -b` clean, graph confirmed to compile.
  - Nothing in this session touched Track 4's actual strategy logic or Track 1's confluence math — all three items are cross-cutting infrastructure (credentials, latency, safety gate), verified live against the real backend/Alpaca connection at each step, not just unit-tested in isolation.
  - **Pre-open live test (same day, ~1.5h before the 9:30am ET open) caught a real reliability gap the instrumentation was built to catch.** Started Track 1 against all 15 symbols to observe behavior before the actual open. Warmup pass: ~15s for all 15 symbols (down from ~2min pre-latency-work, confirming yesterday's fix holds up live), `news_blackout_gate` correctly clear (0ms, no event today). A transient websocket drop (`WinError 121`, a Windows socket timeout) self-recovered via alpaca-py's own reconnect logic within ~25s — expected, no code change needed. Right after that reconnect, one cycle's `quant_engine` stage took **39.3 seconds** (total cycle 42.6s, correctly flagged as exceeding the 2s budget by the WARNING added yesterday). Root cause, confirmed by reading `alpaca.common.rest.RESTClient` directly: it calls `self._session.request(...)` with **no `timeout` anywhere in its own code** — a stalled/dead connection (plausibly the same underlying network blip that dropped the websocket) can hang for as long as the OS takes to notice, not fail fast. Fixed by patching `functools.partial(timeout=15)` onto each cached client's `_session.request` right after construction (`rest_client.py`'s `_with_default_timeout`) — the only available hook, since alpaca-py exposes no constructor param for this; confirmed safe (alpaca-py's own `_one_request` never passes `timeout` itself, so no keyword collision) and confirmed working (`session.request.keywords["timeout"] == 15`, a real `get_account()` call still succeeds). `_retry_once_on_disconnect` widened to also catch `requests.exceptions.Timeout` (a sibling of `ConnectionError`, not previously covered), same one-retry treatment. 71 tests stayed green; Track 1 restarted (pid 11792) to pick up the fix before the real open.
  - **Two more findings from continued pre-open observation, both resolved.** (1) User noticed multi-minute stretches with zero log activity across all 15 symbols and asked whether it was a bug or Alpaca rate-limiting. Verified directly rather than guessed: pulled fresh 1-minute bars via REST for the quiet symbols — several (AAPL, MSFT, GOOGL, AMZN, META, IWM, AMD, COIN) hadn't printed a single new bar in ~4.5 hours despite the request happening at 8:36am ET. Root cause: this account is on Alpaca's free IEX-only feed, not the paid consolidated SIP feed — IEX is one exchange, not the whole tape, and sees genuinely thin-to-zero pre-market flow in most names; no trade means no bar means no websocket message, faithfully. Not a code bug, not rate-limiting (streaming subscriptions aren't quota-limited the way REST calls are) — expected to improve materially at the 9:30am ET open, and if gaps persist deep into regular hours too, the real fix would be upgrading to the paid SIP feed, not a code change. (2) A screenshot showed the same `[SPY] news_blackout_gate: 0ms` line repeated 11 times at one timestamp — checked the raw log file directly and confirmed it only appears once there; the duplication was a frontend-only rendering bug. Root cause in `LogsPage.tsx`: its 1-second `setInterval` poll had no guard against overlapping requests — if any single `/api/logs/tail` call took longer than 1s (exactly what a busy backend, like during the websocket reconnect/39s-stall window, would cause), a second poll fired before the first's response returned, both read the same stale `offsetRef.current`, both got back the identical batch of new lines, and both appended them. Fixed with a simple in-flight guard (skip starting a new poll while one's still pending). `tsc -b` clean.
  - Track 1 stopped again per the user's call — pre-market IEX data is too sparse to learn much more right now; plan is to relaunch ~10 minutes before the 9:30am ET open.
